'use client';import {useEffect,useState} from 'react';import Link from 'next/link';
export default function CapturePayment(){const [status,setStatus]=useState('Verificando con PayPal…');useEffect(()=>{const orderId=new URLSearchParams(location.search).get('token');if(!orderId){setStatus('No se recibió una orden de PayPal.');return}fetch('/api/paypal/capture',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({orderId})}).then(async r=>{const data=await r.json();if(!r.ok)throw Error(data.error);setStatus('Pago confirmado. Tu acceso está activo.')}).catch(e=>setStatus(e.message))},[]);return <><p role="status">{status}</p><Link className="button" href="/dashboard">Ir a mi espacio</Link></>}
