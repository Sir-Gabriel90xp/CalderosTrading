import {updatePassword} from '@/app/actions';export default async function Update({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="shell"><div className="auth-card"><h1>Nueva contraseña</h1>{p.error&&<p className="notice warning">No se pudo actualizar. Usa al menos 10 caracteres.</p>}<form action={updatePassword}><label>Contraseña nueva<input type="password" name="password" minLength={10} required autoComplete="new-password"/></label><button>Guardar contraseña</button></form></div></main>}
