import {supabaseConfigured} from '@/lib/config';import ServiceUnavailable from '@/components/ServiceUnavailable';
import Link from 'next/link';import {signUp} from '@/app/actions';
export default async function Register({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;if(!supabaseConfigured())return <ServiceUnavailable/>;return <main className="shell"><div className="auth-card"><span className="eyebrow">CalderosTrading</span><h1>Crea tu cuenta.</h1><p>Empieza con tu espacio de formación.</p>{p.error&&<div className="notice warning">Introduce un correo válido y una contraseña de al menos 10 caracteres.</div>}<form action={signUp}><label>Correo electrónico<input name="email" type="email" required autoComplete="email"/></label><label>Contraseña<input name="password" type="password" minLength={10} required autoComplete="new-password"/></label><button>Registrarme</button></form><p className="tiny" style={{marginTop:22}}>¿Ya tienes cuenta? <Link href="/login">Iniciar sesión</Link></p></div></main>}
